{
  "name": "matchday-command",
  "version": "1.0.0",
  "private": true,
  "description": "FIFA World Cup 2026 Stadium Operations Platform",
  "scripts": {
    "dev:pwa": "npm run dev --prefix frontend/fan-pwa",
    "dev:dashboard": "npm run dev --prefix frontend/ops-dashboard",
    "dev:backend": "cd backend && uvicorn app.main:app --port 8080 --reload",
    "test:backend": "cd backend && pytest tests/ -v --cov=app",
    "test:pwa": "npm run test --prefix frontend/fan-pwa",
    "test:dashboard": "npm run test --prefix frontend/ops-dashboard",
    "test:all": "npm run test:backend && npm run test:pwa && npm run test:dashboard",
    "build:pwa": "npm run build --prefix frontend/fan-pwa",
    "build:dashboard": "npm run build --prefix frontend/ops-dashboard",
    "install:all": "npm install --prefix frontend/fan-pwa && npm install --prefix frontend/ops-dashboard"
  }
}
